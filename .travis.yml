language: node_js
node_js:
  - "0.10"
services:
  - redis-server
before_install:
  - npm install -g grunt-cli
  - npm install -g coveralls
install: npm install
before_script: grunt default
script: "npm run-script ci"
notifications:
  email: false
